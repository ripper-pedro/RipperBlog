# Hugo build artifacts — generated by `hugo`, never commit
public/
resources/

# The themes symlink is local-only; the hugoThemes builder sets up its own theme path. Don't commit it.
/themes/

# OS / editor
.DS_Store
*.swp
*~